# 使用
参考文件夹`example-usage`中的`README.md`来添加一个题目并测试提交。

# 介绍
整个系统使用容器部署，首次部署只需要运行

```bash
docker-compose up -d --build
```

部署成功可以用`docker-compose ps`看到下面的6个容器：
1. nginx：作为反向代理服务器，统一处理所有前后端请求
2. db：使用postgresql镜像作为整个系统的数据库
3. redis：使用redis做任务队列的管理
4. backend-api：管理后端API
5. backend-worker：接受backend-api的命令，利用宿主机dockerd来创建一个同级容器运行题目的测试
6. frontend：管理系统前端


部署时和环境相关的设置：
1. docker-compose.yml：
    1. 这个文件里规定了前端上传的文件（用户答案，题目测试脚本等）需要放在宿主机的什么位置，然后映射到容器中。这个变量是`HOST_UPLOAD_DIR`，在backend-worker的environment中设置，**必须根据实际部署路径调整**（例如：`/home/用户名/项目路径/data/uploads`）
    2. 系统通过 Nginx 统一对外提供服务，默认使用 **80 端口**：
        - 前端页面：`http://服务器IP/`
        - 后端API：`http://服务器IP/api/`
        - 上传文件：`http://服务器IP/uploads/`
        - API文档：`http://服务器IP/docs`
    3. 如果需要修改对外端口（例如改为 8080）：
        - 修改 docker-compose.yml 中 nginx 服务的端口映射：将 `"80:80"` 改为 `"8080:80"`
        - 前后端容器的端口无需修改，它们通过内部网络与 Nginx 通信
    4. 如果使用端口转发（如 `localhost:81 → 服务器:80`），需要确保 nginx 配置中的 `proxy_redirect off;` 已设置

如果需要添加问题，那么需要准备一个用于测试的镜像，然后准备一个用于测试的脚本。

# 题目配置说明

## 提交文件与映射路径

创建或编辑题目时，`Submission Mapping Path` 决定学生提交的答案文件在评测容器中的位置：

1. 留空时使用默认值：`/input/submission.zip`
2. 填写具体文件路径时，保持原逻辑，将提交文件挂载到这个固定路径，例如：`/input/submission.zip`
3. 填写目录路径时，系统会保留学生上传的原始文件名，并挂载到这个目录下，例如：
    - 学生提交：`20240001.zip`
    - 映射路径：`/workspace/` 或 `/workspace`
    - 容器内路径：`/workspace/20240001.zip`

目录路径的判断规则是：路径以 `/` 结尾，或者路径最后一段没有扩展名。需要固定文件名时，请填写带文件名和扩展名的完整路径。

答案文件当前支持这些扩展名：`.zip`、`.tar`、`.gz`、`.tar.gz`、`.csv`、`.txt`、`.json`、`.py`、`.patch`、`.diff`。提交文件会保留原始文件名保存，报告文件仍会使用 `report_` 前缀保存。

这个目录映射适合需要从文件名获取学生信息的题目。例如要求学生用学号命名答案文件，然后将 `Submission Mapping Path` 配置为 `/workspace/`，测试脚本即可在容器内读取 `/workspace` 下的原始文件名。

## 成绩导出

管理员可以在用户管理页面导出 CSV，内容包含每个用户在每道题上的最佳分数和最佳性能指标。默认不包含管理员账号的提交，可以通过页面上的 `Include Admin` 开关包含管理员数据。

# 设计

本系统只专注于下面这些事：
1. 接受用户对不同题目的答案提交，然后根据题目要求，使用测试镜像运行一个容器，将测试脚本作为这个容器的CMD，容器后直接执行这个脚本。
2. 系统完全不在意测试过程，系统只负责收集容器的logs，然后在最终的logs中解析是否有"SCORE: "字样，并把后面的数字作为分数存入数据库，显示在榜单中。其中日志也可以在榜单中看到。
3. 清晰的划分不同用户对于不同题目的不同次数的提交，这些提交保存在本代码根目录下的./data/uploads文件夹。提交的答案保留原始文件名，报告使用report_前缀标记。具体结构：`uploads/{user_id}/{problem_id}/{uuid}/`
4. 让管理员可以管理用户，管理提交记录，管理题目。普通用户不能管理几乎任何东西，但是可以修改自己的密码。
5. 让系统一次只运行一个用户的测试请求，维护一个测试队列。
6. 提供看板，用户可以看到自己的和其他人的提交记录，包含分数、提交时间、提交的题目、用户名。不能看到其他人的日志。
7. 可以对提交记录做一些简单的过滤和排序，比如按照题目、用户名过滤，按照时间、成绩排序。

所有不在上面范围内的事情，都应该交给测试时完成。比如如何判断学生的得分。可以在测试脚本末尾运行"判断脚本"来判断这个学生应该拿多少分。

比如：
- 对于功能测试：完成功能就累计分数，最后"判断脚本"输出一个总分
- 对于带有指标的测试：对指标合理划分，利用函数曲线映射或者离散划分成绩，最后"判断脚本输出一个总分"

总之，测试脚本需要负责测试，并且计算这次有多少分，然后输出一个简单的"SCORE: xxx"告诉测试系统，这个同学的这次提交得了xxx分
